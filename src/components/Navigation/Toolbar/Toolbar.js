import React from "react";
import classes from "./Toolbar.module.css"

const Toolbar = (props)=>{

    return (
        <header>
            <div>Menu</div>
            <div>Logo</div>

            <nav>
                    ...
            </nav>
        </header>
    )

}


export default Toolbar;